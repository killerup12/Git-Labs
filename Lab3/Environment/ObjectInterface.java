package Environment;

interface ObjectInterface
{
    String DegreeOfIllumination(int degreeOfIllumination);    //Checks the degree of illumination
     String Pronoun(String gender);    //changes something to the corresponding pronoun
      String Prepositions(String pronoun);    //Puts the appropriate preposition
       String RelativeLevel(int height1, int height2);    //compares the height of different subjects
      String EmilASmell(int odorConcentration);
}
